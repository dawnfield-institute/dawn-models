# Ember III deep-documentation phase (dawn-models side)

**Date:** 2026-07-20

- `research/ember3_scale160m/journals/2026-07-20_program_synthesis.md` — the
  consolidated rounds 1–10 map: round table with verdicts + journal index, the
  five keeper results, four retractions on record, instrument inventory,
  node1/CT103 asset registry, prioritized open queue.
- `research/ember3_scale160m/README.md` — rounds 7–10 results added (twins,
  MoE, retention verdict, compensation law) + pointers to synthesis/founding docs.
- `research/scbf/README.md` — rewritten (repairing pre-existing header
  corruption), with a full v2 section + quick-start example. `research/scbf/v2/meta.yaml` added.
- Lore decomposition (graph hygiene, one concept per FDO): new nodes
  `compensation-law`, `gradient-pressure-lesioning`, `scbf-v2`, all linked to the
  arc node `ember3-drift-shakedown` (which remains the index) and prior art
  (`tinycimm-boltzmann`, `ember-poc1`, `scbf-framework`, `proj-cradle`).

Companion cradle-side entry: `cradle/.changelog/` (founding doc capture + addendum).
